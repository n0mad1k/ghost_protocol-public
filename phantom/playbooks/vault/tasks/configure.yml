---
# Vaultwarden Docker configuration

- name: Display Vaultwarden admin panel info
  debug:
    msg: "Vaultwarden admin panel at https://{{ vaultwarden_domain }}/admin — token: {{ vaultwarden_admin_token }}"
